<h1>Users</h1>
<ul id="users">
  <% @users.each do |user| %>
    <li>
      <p>
        Username: <%= user.username %><br />Email: <%= user.email %> <%= link_to 'View user', user_path(user) %>
      </p>
    </li>
  <% end %>
  
</ul>

<%= link_to 'Create a New User', new_user_path %>